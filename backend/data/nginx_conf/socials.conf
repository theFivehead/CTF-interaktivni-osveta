server {
        listen 80;
        listen [::]:80;

        root /var/www/slowrgram/;

        # Add index.php to the list if you are using PHP
         index index.html;

        server_name slowrgram.com;

        location / {
                # First attempt to serve request as file, then
                # as directory, then fall back to displaying a 404.
                try_files $uri $uri/ =404;
        }
}

server {
        listen 80;
        listen [::]:80;

        root /var/www/syncdln/;

        # Add index.php to the list if you are using PHP
         index index.html;

        server_name syncdln.com;

        location / {
                # First attempt to serve request as file, then
                # as directory, then fall back to displaying a 404.
                try_files $uri $uri/ =404;
        }
}

server {
        listen 80;
        listen [::]:80;

        root /var/www/threat-analysis/;

        # Add index.php to the list if you are using PHP
         index index.php index.html;

        server_name threat-analysis.sos;

        # pass PHP scripts to FastCGI server
        #
        location ~ \.php$ {
               include snippets/fastcgi-php.conf;
        #
        #       # With php-fpm (or other unix sockets):
               fastcgi_pass unix:/run/php/php8.4-fpm.sock; #pokud mate jinou verzi php, zmente cislo
        #       # With php-cgi (or other tcp sockets):
        #       fastcgi_pass 127.0.0.1:9000;
        }

        location / {
                # First attempt to serve request as file, then
                # as directory, then fall back to displaying a 404.
                try_files $uri $uri/ =404;
        }
        
}